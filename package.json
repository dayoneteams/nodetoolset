{
  "name": "nodetoolkit",
  "version": "0.0.0",
  "description": "Setup of utility commands. Built with love for nodejs developers",
  "bin": "src/remove-nodemodules.ts",
  "main": "build/src/index.js",
  "types": "build/src/index.d.ts",
  "files": [
    "build/src"
  ],
  "license": "MIT",
  "keywords": [
    "utility",
    "nodejs"
  ],
  "scripts": {
    "start:dev": "ts-node src/remove-nodemodules.ts --help",
    "test": "echo \"Error: no test specified\" && exit 1",
    "check": "gts check",
    "clean": "gts clean",
    "compile": "tsc -p .",
    "fix": "gts fix",
    "prepare": "npm run compile",
    "pretest": "npm run compile",
    "posttest": "npm run check"
  },
  "dependencies": {
    "commander": "^2.20.0"
  },
  "devDependencies": {
    "@types/node": "^11.13.8",
    "gts": "^1.0.0",
    "ts-node": "^8.1.0",
    "typescript": "~3.4.0"
  }
}
